"""Компиляция пользовательских типов из текста в исполнимую спецификацию.

`compiler.py` — вызов LLM через `masker.llm.LLMProvider` и разбор одного из
четырёх исходов (design notes T1.13, раздел 2.3). `prompt.py` — только
построение запроса. `preview.py` — прогон скомпилированного типа по
настоящим сегментам документа. `graph.py` — LangGraph-граф компиляции со
своим чекпойнтером (план T1.13, шаг 11).
"""

from __future__ import annotations
