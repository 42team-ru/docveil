"""Контракт поставщика OCR.

Один метод: изображение страницы → список распознанных строк с геометрией.
Провайдер видит именно изображение, а не PDF — модуль ingest сам решает,
какая страница пойдёт на OCR, и рендерит её в ``numpy.ndarray`` через
PyMuPDF. Так провайдер не тащит зависимость от pymupdf и подходит для
любого другого источника изображения (например, скан из очереди).

Координаты в :class:`OCRLine.bbox` — **пиксельные** координаты входного
изображения (см. :meth:`OCRProvider.recognize`). Пересчёт в pt страницы
делает ingest (``scan_ingest.py``), потому что только он знает исходный
``dpi`` рендера и трансформации страницы (rotation, mediabox). Класть
пересчёт в провайдер значит склеить контракт с одним конкретным
источником изображений.
"""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Protocol, runtime_checkable

import numpy as np


class OCRError(RuntimeError):
    """Провайдер OCR не смог распознать изображение.

    Единый корень для ошибок разных движков (Paddle/Tesseract/Fake) —
    чтобы вызывающий не проверял ``ImportError``/``FileNotFoundError``
    от конкретной реализации. Отсутствие пакета движка тоже поднимается
    как ``OCRError`` с понятным текстом про ``pip install
    triema-masker[ocr]`` (см. ``masker.ocr.select``).
    """


@dataclass(frozen=True, slots=True)
class OCRLine:
    """Одна распознанная строка: текст, полигон, ограничивающий прямоугольник, уверенность.

    ``bbox`` — axis-aligned прямоугольник в пикселях исходного изображения
    (``x0, y0, x1, y1``), ``polygon`` — четыре угла реального (возможно
    повёрнутого) четырёхугольника в тех же пиксельных координатах, обход
    по часовой стрелке от левого верхнего. Полигон нужен ingest'у, чтобы
    после автодескью (страница повёрнута на 90°/180°/270°) точно
    отобразить область в pt-координатах страницы; ``bbox`` — дешёвая
    геометрия для случаев, когда достаточно axis-aligned (детекция,
    расчёт покрытия). Оба поля обязательны — их всегда возвращает
    сегодняшний PaddleOCR, а тесты сравнения провайдеров становятся
    независимыми от того, что именно есть в свежем ответе движка.

    ``confidence`` — в диапазоне ``[0.0, 1.0]``, чем ближе к 1, тем
    увереннее. Точная семантика зависит от движка (Paddle возвращает
    softmax по строке); порогов на неё в детекции пока нет.
    """

    text: str
    bbox: tuple[float, float, float, float]
    polygon: tuple[
        tuple[float, float], tuple[float, float], tuple[float, float], tuple[float, float]
    ]
    confidence: float = 1.0
    #: Индекс строки в порядке, который вернул движок (top-to-bottom,
    #: left-to-right у Paddle). Хранится отдельно, чтобы порядок сегментов
    #: в ingest'е не зависел от способа обхода списка.
    order: int = 0
    #: Дополнительные поля, специфичные для движка (например, номер строки
    #: у PaddleOCR или id полигона). Не участвуют в контрактах ingest'а,
    #: не сериализуются в стейт — только диагностика.
    extra: dict[str, object] = field(default_factory=dict)


@runtime_checkable
class OCRProvider(Protocol):
    """Единственный контракт входа в OCR.

    Реализация обязана быть **потокобезопасной на чтение**: один
    ``OCRProvider`` создаётся на процесс и обходит все страницы (ленивая
    инициализация моделей внутри — норма). Изменение внутреннего
    состояния между вызовами ``recognize`` не разрешено — иначе повторный
    прогон обезличенного файла (инвариант идемпотентности из ``AGENTS.md``)
    даст разный результат.
    """

    def recognize(self, image: np.ndarray, dpi: int) -> tuple[OCRLine, ...]:
        """Распознать одно изображение (страница) и вернуть строки в порядке движка.

        ``image`` — RGB или BGR-массив формы ``(H, W, 3)`` в ``uint8``;
        конкретика зависит от движка, ingest готовит BGR (стандарт
        PaddleOCR/OpenCV) — единственная сегодня используемая раскладка.
        ``dpi`` — с каким dpi ingest отрендерил страницу; провайдер не
        обязан его использовать, но обязан принять (сюда естественно
        уходят движки, которые калибруют свои пороги через физический
        размер).

        Пустой список — валидный результат («на странице ничего не
        распознали»), исключение — только фатальная ошибка движка.
        """
        ...
