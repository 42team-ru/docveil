# Голый origin бэкенда, БЕЗ префикса /api — его добавляет authMutator.ts
# (baseURL = origin + "/api") ровно один раз для всех запросов.
VITE_BACKEND_DEV_URL=http://localhost:8000
VITE_BACKEND_PROD_URL=https://42team.ru