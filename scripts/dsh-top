#!/usr/bin/env bash
# Persistent terminal-first entrypoint for the project dsh-TUI profile.
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT/scripts/dsh.sh" --profile dsh-tui "$@"
